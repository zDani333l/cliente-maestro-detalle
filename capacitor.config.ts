import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cliente-maestro-detalle',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
